entity: UserEntity
items:
  user{1..2}:
    password: "{{internet.password}}"
    name: "{{person.lastName}}"
    firstname: "{{person.firstName}}"
    email: "{{internet.email}}"
    adress: "{{location.streetAddress}}"
    phone: "06 01 02 03 04"
    role: "CLIENT"

  user{3}:
    password: "{{internet.password}}"
    name: "{{person.lastName}}"
    firstname: "{{person.firstName}}"
    email: "{{internet.email}}"
    adress: "{{location.streetAddress}}"
    phone: "06 01 02 03 04"
    role: "CLIENT"

  user{4}:
    password: "{{internet.password}}"
    name: "{{person.lastName}}"
    firstname: "{{person.firstName}}"
    email: "{{internet.email}}"
    adress: "{{location.streetAddress}}"
    phone: "06 01 02 03 04"
    role: "CLIENT"
